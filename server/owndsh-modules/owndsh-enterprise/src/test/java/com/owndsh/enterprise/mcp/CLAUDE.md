# mcp/

> L2 | 父级: ../../../../../CLAUDE.md

成员清单

McpGrantIntegrationTest.java: PostgreSQL/MockMvc 验证授权并集/回收、CAS 并发胜负、noop、404/409、事务回滚、组删除引用释放、创建幂等重放与 tenant 隔离；覆盖 HTTP OAuth 创建/更新/授权分发和非法 URL 拒绝、固定 Header/单认证头元数据分发、拒绝前缀和保留头、创建/分页/启停/删除协议，不覆盖员工端连接撤销。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
