{
  "name": "owndsh-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pake desktop carrier for official DeepSeek Harness and OwnDsh",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "prepare:runtime": "node build.mjs --prepare-only",
    "build": "node build.mjs",
    "test": "node --test launcher.test.mjs"
  },
  "devDependencies": {
    "pake-cli": "3.16.1"
  }
}
