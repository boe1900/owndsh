# @enterprise-agent/dsh-ui

Browser-side enterprise surface. T01 registers one compact
`sidebar.footer.action` through the official Client slot runtime and reads the
Host's same-origin status endpoint. The component receives a fetch capability
through its slot injection closure; no Host context enters React.
