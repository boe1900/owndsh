# @enterprise-agent/dsh-llm-gateway

OpenAI-compatible SSE decoding boundary for the future enterprise LLM adapter.
T01 proves incremental frame parsing, structured upstream errors, premature
disconnect detection, and cancellation without implementing model routing.
