# tests/

> L2 | 父级: ../CLAUDE.md

成员清单

deployment.test.mjs: 解析 Docker Compose JSON 并检查镜像 digest、端口、bootstrap 隔离、deploy 占位配置、真实换行注入、secret、只读 Gateway 临时写入、代理头、脚本语法与回滚边界。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
