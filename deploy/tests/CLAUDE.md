# tests/

> L2 | 父级: ../CLAUDE.md

成员清单

deployment.test.mjs: 解析 Docker Compose JSON 并检查镜像 digest、HTTP Console、Server Jammy JRE、Harness 锁定 manifest 与升级/回滚 bundle 对齐、本地缓存反查、bootstrap 隔离、可移植 SHA-256、secret、OpenAPI/SPA 路由、上级代理协议传递、运维脚本与源码 CLI 可调和边界。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
