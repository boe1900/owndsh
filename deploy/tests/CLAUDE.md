# tests/

> L2 | 父级: ../CLAUDE.md

成员清单

deployment.test.mjs: 解析 Docker Compose JSON 并检查镜像 digest、Server Jammy JRE 与 Fontconfig 临时缓存、本地缓存反查、端口、bootstrap 隔离、deploy 占位配置、真实换行注入、可移植 SHA-256、secret、OpenAPI/SPA 路由边界、只读 Gateway、代理头、运维脚本与单 Harness 本地体验边界。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
