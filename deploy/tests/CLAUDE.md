# tests/

> L2 | 父级: ../CLAUDE.md

成员清单

deployment.test.mjs: 解析 Docker Compose JSON 并检查镜像 digest、Server Jammy JRE、Console、Harness 锁定 manifest 与升级/回滚 bundle 对齐、本地缓存反查、端口、bootstrap 隔离、可移植 SHA-256、secret、OpenAPI/SPA 路由边界、只读前端容器、代理头、运维脚本与源码 CLI 可调和的单 Harness 本地体验边界。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
